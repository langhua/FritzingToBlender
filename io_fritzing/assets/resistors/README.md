运行create_resistor_color_icons.py在icons目录下生成界面显示电阻色环所需的图标。

create_resistor_color_icons.py中使用了Pillow库，请先安装：

```
pip install Pillow
```

然后运行create_resistor_color_icons.py生成图标：

```
python create_resistor_color_icons.py
```

进入resistor_icons目录下，检查生成的图标是否正确。
